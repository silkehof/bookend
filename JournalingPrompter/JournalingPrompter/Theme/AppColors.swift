import SwiftUI

extension Color {
    // Primary warm colors
    static let warmAccent = Color(red: 0.91, green: 0.56, blue: 0.44) // Soft coral
    static let warmSecondary = Color(red: 0.96, green: 0.76, blue: 0.56) // Warm peach
    static let warmTertiary = Color(red: 0.98, green: 0.91, blue: 0.82) // Cream

    // Gradient colors
    static let warmGradientStart = Color(red: 0.95, green: 0.65, blue: 0.50) // Soft orange
    static let warmGradientEnd = Color(red: 0.85, green: 0.50, blue: 0.55) // Dusty rose

    // Background colors
    static let warmBackground = Color(red: 0.99, green: 0.97, blue: 0.94) // Warm white
    static let warmCardBackground = Color(red: 0.97, green: 0.94, blue: 0.90) // Light cream

    // Text colors
    static let warmText = Color(red: 0.35, green: 0.28, blue: 0.25) // Warm brown
}
